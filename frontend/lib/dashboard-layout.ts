/** Fixed width of the global nav column (dashboard). Keep in sync with GlobalNav layout. */
export const GLOBAL_NAV_WIDTH_PX = 220;

/** Deal terms column width on `/offers/[id]` (DealSidebar). */
export const DEAL_OFFER_SIDEBAR_WIDTH_PX = 360;

/** Right context sidebar (active deals) width when open. */
export const RIGHT_CONTEXT_SIDEBAR_WIDTH_PX = 260;
